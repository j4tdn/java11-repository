package exam;

import static utils.FileUtils.*;

import common.Extension;

import static common.Extension.*;

public class Ex01 {
	private static final String dirPath = "garbage";
	private static final String dirPathMusic = "garbage/music";
	private static final String dirPathImage = "garbage/image";
	private static final String dirPathSystem = "garbage/system";
	private static final String dirPathCoding = "garbage/coding";
	
	public static void main(String[] args) {
//		create(dirPath, 20, values());
		
		// move file .mp4, .mp3 => folder music
//		move(dirPath, dirPathMusic, mp4 );
//		move(dirPath, dirPathMusic, mp3 );
		
		// move file .png, .jpg, .jpeg 	=> folder image
//		move(dirPath, dirPathImage, png );
//		move(dirPath, dirPathImage, jpg );
//		move(dirPath, dirPathImage, jpeg );
		
		// move file .bat	=> folder system
//		move(dirPath, dirPathSystem, bat );
		
		// copy file .java => folder coding
//		copy(dirPath, dirPathCoding, java );
		
		
		// delete file .txt 
//		delete(dirPath, txt);
	}


}
